# --- REQUIRED ---
SSH_HOST=192.168.1.100  # Replace with your Samba NAS IP or Hostname
SSH_PORT=22
SSH_HOST_KEY_FINGERPRINT=

# --- SECURITY (auto-generated on first run if empty) ---
JWT_SECRET=
VAULT_KEY=

# --- TUNING ---
SESSION_TTL_MINUTES=30
MAX_CONCURRENT_SESSIONS=50
LOGIN_RATE_LIMIT_PER_15MIN=5
API_RATE_LIMIT_PER_MIN=60
ALLOWED_SHARE_ROOTS=/srv/samba

# --- OPTIONAL ---
AUDIT_LOG_PATH=./audit.log
